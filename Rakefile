# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/gserv.rb'
require './lib/tcpserv.rb'

Hoe.new('gserv', Gserv::VERSION) do |p|
  # p.rubyforge_name = 'gservx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
